SET(vis_SRCS matches_page.cxx)

ADD_LIBRARY(vis ${vis_SRCS})
INCLUDE_DIRECTORIES(vis ${VOCAB_TREE_INCLUDE} ${BOOST_INCLUDE_PATH} ${OPENCV_INCLUDE_PATH})
TARGET_LINK_LIBRARIES(vis utils ${BOOST_LIBRARIES} ${OPENCV_LIBRARIES})
SET_TARGET_PROPERTIES(vis PROPERTIES COMPILE_FLAGS -fPIC)